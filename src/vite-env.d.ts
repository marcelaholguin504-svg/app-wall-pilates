/// <reference types="vite/client" />

// Definida en vite.config.ts vía `define`, con el hash corto del commit
// que generó este build (ver Profile.tsx, marca de versión visible).
declare const __APP_COMMIT_HASH__: string;
